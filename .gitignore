# Root-level ignores; backend/ and frontend/ have their own more specific .gitignore too.
.env
*.log
.DS_Store
.idea/
*.iml

# Raw GPX dump -- the canonical copy used by the app lives in
# backend/src/main/resources/gpx/ (normalized filenames, actually bundled into the build).
/stages/
